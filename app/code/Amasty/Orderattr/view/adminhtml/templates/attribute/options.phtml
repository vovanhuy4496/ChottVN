<?php
/**
 * @author Amasty Team
 * @copyright Copyright (c) 2020 Amasty (https://www.amasty.com)
 * @package Amasty_Orderattr
 */
?>
<?php
/** @var $block \Amasty\Orderattr\Block\Adminhtml\Attribute\Edit\Tab\Options\Options */
?>
<fieldset class="fieldset-wrapper admin__collapsible-block-wrapper">
    <div class="fieldset-wrapper-title">
        <strong class="admin__collapsible-title" data-toggle="collapse" data-target="#manage-options-panel">
            <span><?= $block->escapeHtml(__('Manage Options (values of your attribute)')); ?></span>
        </strong>
    </div>
    <div class="fieldset-wrapper-content in collapse selected-type-options" id="manage-options-panel">
        <div id="manage-options-panel">
            <table class="admin__control-table">
                <thead>
                    <tr id="attribute-options-table">
                        <th class="col-draggable"></th>
                        <th class="col-default control-table-actions-th">
                            <?= /* @escapeNotVerified */ __('Is Default') ?>
                            <div>(
                                <a class="am-unselect-default"><?= /* @escapeNotVerified */ __('Unselect All') ?></a>
                                )
                            </div>
                        </th>
                        <?php foreach ($block->getStores() as $store) : ?>
                            <th<?= ($store->getId() == \Magento\Store\Model\Store::DEFAULT_STORE_ID)?' class="_required"':''; ?>>
                                <span><?= $block->escapeHtml(__($store->getName())); ?></span>
                            </th>
                        <?php endforeach;
                        $storetotal = count($block->getStores()) + 3;
                        ?>
                        <th class="col-delete">&nbsp;</th>
                    </tr>
                </thead>
                <tbody data-role="options-container" class="ignore-validate"></tbody>
                <tfoot>
                <tr>
                    <th colspan="<?= /* @escapeNotVerified */ $storetotal; ?>" class="validation">
                       <input type="hidden" class="required-dropdown-attribute-entry" name="dropdown_attribute_validation"/>
                    </th>
                </tr>
                <tr>
                    <th colspan="<?= /* @escapeNotVerified */ $storetotal; ?>" class="col-actions-add">
                        <?php if (!$block->getReadOnly() && !$block->canManageOptionDefaultOnly()) :?>
                            <button id="add_new_option_button"
                                    title="<?= /* @escapeNotVerified */ __('Add Option'); ?>"
                                    type="button" class="action- scalable add">
                                 <span><?= /* @escapeNotVerified */ __('Add Option'); ?></span>
                            </button>
                        <?php endif; ?>
                    </th>
                </tr>
                </tfoot>
            </table>
            <input type="hidden" id="option-count-check" value="" />
        </div>
    </div>
    <script id="row-template" type="text/x-magento-template">
         <tr>
            <td class="col-draggable">
                <?php if (!$block->getReadOnly() && !$block->canManageOptionDefaultOnly()) : ?>
                    <div data-role="draggable-handle" class="draggable-handle"
                         title="<?= /* @escapeNotVerified */ __('Sort Option'); ?>"></div>
                <?php endif; ?>
                <input data-role="order" type="hidden" name="option[order][<%- data.id %>]"
                       value="<%- data.sort_order %>"
                        <?php if ($block->getReadOnly() || $block->canManageOptionDefaultOnly()) : ?>
                            disabled="disabled"
                        <?php endif; ?>
                />
            </td>
            <td class="col-default control-table-actions-cell">
                <input class="input-radio" type="<%- data.intype %>" name="default[]"
                       value="<%- data.id %>" <%- data.checked %>
                <?php if ($block->getReadOnly()) :?>
                    disabled="disabled"
                <?php endif;?>
                />
            </td>
            <?php foreach ($block->getStores() as $store) : ?>
            <td class="col-<%- data.id %>">
                <input  name="option[value][<%- data.id %>][<?= /* @escapeNotVerified */ $store->getId() ?>]"
                        value="<%- data.store<?= /* @escapeNotVerified */ $store->getId() ?> %>"
                        class="input-text
                        <?php if ($store->getId() == \Magento\Store\Model\Store::DEFAULT_STORE_ID) :?>
                          required-option
                        <?php endif; ?>"
                        type="text"
                        <?php if ($block->getReadOnly() || $block->canManageOptionDefaultOnly()) : ?>
                           disabled="disabled"
                        <?php endif;?>
                />
            </td>
            <?php endforeach; ?>
            <td id="delete_button_container_<%- data.id %>" class="col-delete">
                <input type="hidden" class="delete-flag" name="option[delete][<%- data.id %>]" value="" />
                <?php if (!$block->getReadOnly() && !$block->canManageOptionDefaultOnly()) :?>
                    <button id="delete_button_<%- data.id %>"
                            title="<?= /* @escapeNotVerified */ __('Delete') ?>" type="button"
                            class="action- scalable delete delete-option"
                        >
                        <span><?= /* @escapeNotVerified */ __('Delete') ?></span>
                    </button>
                <?php endif;?>
            </td>
        </tr>
    </script>
    <?php
    $values = [];
    foreach ($block->getOptionValues() as $value) {
        array_push($values, $value->getData());
    }
    ?>
    <script type="text/javascript">
        require([
            "jquery"
        ], function (jQuery) {
            jQuery('body').on(
                {'click': function(){
                    jQuery('input[name="default[]"]').removeAttr('checked');
                }},
                ".am-unselect-default"
            );
        });
    </script>
    <script type="text/x-magento-init">
        {
            "*": {
                "Magento_Catalog/js/options": {
                    "attributesData": <?= /* @escapeNotVerified */ json_encode($values); ?>,
                    "isSortable":  <?= (int)(!$block->getReadOnly() && !$block->canManageOptionDefaultOnly()) ?>,
                    "isReadOnly": <?= (int)$block->getReadOnly(); ?>
                }
            }
        }
    </script>
</fieldset>
